POSTGRES_USER=dbdash
POSTGRES_PASSWORD=dbdash_dev
POSTGRES_DB=dbdash
DATABASE_URL=postgresql://dbdash:dbdash_dev@localhost:5432/dbdash


# NL-to-SQL requires `claude` CLI to be installed and authenticated.
# Run `claude login` to authenticate via your Claude Pro/Max subscription.
